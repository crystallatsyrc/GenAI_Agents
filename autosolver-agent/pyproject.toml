[project]
name = "autosolver-agent"
version = "0.1.0"
description = "Budgeted strategy-search agent for stochastic courier assignment."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Ansel" }]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
